[package]
name = "palette_bitmap"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.30.4", default-features = false, features = ["std"] }
serde = { version = "1.0.219", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
